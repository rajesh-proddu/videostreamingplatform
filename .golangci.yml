version: "2"

linters:
  default: standard
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gocritic

formatters:
  enable:
    - gofmt
    - goimports

issues:
  exclude-dirs:
    - dataservice/pb
