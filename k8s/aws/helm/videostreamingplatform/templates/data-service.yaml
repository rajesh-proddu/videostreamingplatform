apiVersion: v1
kind: ServiceAccount
metadata:
  name: data-service
  namespace: videostreamingplatform
  {{- if .Values.dataService.irsa.roleArn }}
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.dataService.irsa.roleArn }}
  {{- end }}

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-service
  namespace: videostreamingplatform
  labels:
    app: data-service
    {{- include "vsp.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.dataService.replicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: data-service
  template:
    metadata:
      labels:
        app: data-service
    spec:
      serviceAccountName: data-service
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - data-service
                topologyKey: kubernetes.io/hostname
      containers:
        - name: data-service
          image: {{ .Values.image.registry }}/data-service:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.dataService.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: app-config
          env:
            - name: HTTP_PORT
              value: {{ .Values.dataService.port | quote }}
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: {{ .Values.observability.jaeger.endpoint | quote }}
          resources:
            {{- toYaml .Values.dataService.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 2
          securityContext:
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false

---
apiVersion: v1
kind: Service
metadata:
  name: data-service
  namespace: videostreamingplatform
  labels:
    app: data-service
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
    service.beta.kubernetes.io/aws-load-balancer-cross-zone-load-balancing-enabled: "true"
spec:
  type: LoadBalancer
  selector:
    app: data-service
  ports:
    - name: http
      port: {{ .Values.dataService.port }}
      targetPort: {{ .Values.dataService.port }}
      protocol: TCP

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: data-service
  namespace: videostreamingplatform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: data-service
  minReplicas: {{ .Values.dataService.hpa.minReplicas }}
  maxReplicas: {{ .Values.dataService.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.dataService.hpa.cpuTarget }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.dataService.hpa.memoryTarget }}
