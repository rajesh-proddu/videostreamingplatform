apiVersion: v1
kind: Service
metadata:
  name: metadata-service
  namespace: videostreamingplatform
  labels:
    app: metadata-service
spec:
  type: LoadBalancer
  selector:
    app: metadata-service
  ports:
    - name: http2
      port: 8080
      targetPort: 8080
      protocol: TCP

---
apiVersion: v1
kind: Service
metadata:
  name: data-service
  namespace: videostreamingplatform
  labels:
    app: data-service
spec:
  type: LoadBalancer
  selector:
    app: data-service
  ports:
    - name: http2
      port: 8081
      targetPort: 8081
      protocol: TCP
